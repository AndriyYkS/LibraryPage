const bookExtent = [];

class Book{
	constructor(title, author, isbn,language){
		this.title = title;
		this.author = author;
		this.isbn = isbn;
		this.language = language;
	}

	static add(book){
		bookExtent.push(book);
		return user;
	}
	
}